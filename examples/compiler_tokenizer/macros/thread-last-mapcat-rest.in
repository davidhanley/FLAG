(defmacro ->> [x & forms]
  (macro-case (do forms)
    ([] x)
    ([(f & args) & rest]
     (->> (f args x) rest))
    ([f & rest]
     (->> (f x) rest))))
(->> groups (mapcat rest) (map identity) (vec))